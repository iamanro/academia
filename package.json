{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
    "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "predeploy": "npm run build", // <-- PŘIDAT TENTO ŘÁDEK
    "deploy": "gh-pages -d dist"  // <-- PŘIDAT TENTO ŘÁDEK
  },
}
